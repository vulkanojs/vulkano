/**
 * Permission Policy
 */
module.exports = {

  // Enabled
  enabled: true,

  // Permissions
  permissions: [
    'accelerometer=()',
    'camera=()',
    'geolocation=()',
    'gyroscope=()',
    'magnetometer=()',
    'microphone=()',
    'payment=()',
    'usb=()'
  ]

};
